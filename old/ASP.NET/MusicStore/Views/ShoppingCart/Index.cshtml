@model IEnumerable<MusicStore.Models.CartItem>

@{
    ViewData["Title"] = "Shopping Cart";

    var total = 0;
}

<h2>Shopping Cart</h2>

<p>
    <h4>Review your albums</h4>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Album)
            </th>
            <th>
                Price (each)
            </th>
            <th>
                Quantity
            </th>
            <th>
                Total
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            total += item.Album.Price * item.Count;
            <tr>
                <td>
                    <a asp-controller="Store" asp-action="Details" asp-route-id="@item.Album.AlbumID">
                        @Html.DisplayFor(modelItem => item.Album.Title)
                        (@Html.DisplayFor(modelItem => item.Album.Artist.Name))
                    </a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Album.Price) euro
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Count)
                </td>
                <td>
                    @(item.Count * item.Album.Price) euro
                </td>
                <td>
                    <a asp-controller="ShoppingCart" asp-action="RemoveFromCart" asp-route-id="@item.CartItemID">Remove from cart</a>
                </td>
            </tr>
        }
    </tbody>
    <thead>
        <tr>
            <th>
                Total
            </th>
            <th>

            </th>
            <th>

            </th>
            <th>
                @(total) euro
            </th>
            <th></th>
        </tr>
    </thead>
</table>

<a asp-controller="Checkout" asp-action="Create">Checkout</a> |
<a asp-controller="Home" asp-action="Index">Back to Home</a>



@*@model IEnumerable<MusicStore.Models.CartItem>

    @{
        ViewData["Title"] = "Shopping Cart";
        var totalPrice = 0;
    }

    <h1>Shopping Cart</h1>
    <h3>Review your albums</h3>

    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Album)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Album.Price) (each)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Count)
                </th>
                <th>Total</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (CartItem cartitem in Model)
            {
                <tr>

                    <td>
                        <a asp-controller="Store" asp-action="Details" asp-route-id="@cartitem.AlbumID">@Html.DisplayFor(modelItem => cartitem.Album.Title)</a>
                    </td>

                    <td>
                        @Html.DisplayFor(modelItem => cartitem.Album.Price) euro
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => cartitem.Count)
                    </td>
                    <td>
                        @{
                            var total = cartitem.Album.Price * cartitem.Count;
                            totalPrice += total;
                        }
                        @total euro

                    </td>
                    <td>
                        <a asp-controller="ShoppingCart" asp-action="RemoveFromCart" asp-route-id="@cartitem.AlbumID">Remove from cart</a>
                    </td>
                </tr>
            }
        </tbody>
        <th>Total</th>
        <th></th>
        <th></th>
        <th>@totalPrice euro</th>
        <th></th>
    </table>
    <a asp-controller="Checkout" asp-action="Create">Checkout</a> |
    <a asp-controller="Home" asp-action="Index">Back to Home</a>*@
